#! /bin/sh
set -e

CDIMAGE_ROOT=/srv/cdimage.no-name-yet.com
. "$CDIMAGE_ROOT/etc/config"
ARCHES='amd64 i386 powerpc' # temporary override

PATH="$CDIMAGE_ROOT/bin:${PATH:+:$PATH}"

export LIVE=1

build-image-set daily-live
